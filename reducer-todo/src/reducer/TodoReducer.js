import React from 'react';

export const initialState = {
    item: 'Test',
    completed: false,
    id: 3892987589
}

export const reducer = (state, action) => {
    switch (action.type) {
        default:
            return state
    }

}